language: android
sudo: required
jdk :
    - oraclejdk8

before_install:
    - chmod +x gradlew
    - chmod +rx app

android:
    components:
        - tools
        - platform-tools
        - tools
        - build-tools-27.0.3
        - android-26
skip_cleanup: true
